import styled from '@emotion/styled';
import { ThemeIcon } from '@mantine/core';
import theme from 'design-system/theme';

export const Teme = styled(ThemeIcon)({
  border: `1px solid ${theme.colors.bordercolor} `,
});
